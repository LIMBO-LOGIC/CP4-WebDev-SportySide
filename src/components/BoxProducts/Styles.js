import styled from "styled-components";

export const BoxProductStyled = styled.div`
  display: flex;
  flex-direction: column;
  width: 100%;
  gap: 12px;

  p {
    font-size: 2rem;
    font-weight: 600;
  }

  .swiper-button-next {
    color: var(--bg-color);
  }

  .swiper-button-prev {
    color: var(--bg-color);
  }

  
`;
